# Shell Plugin Configuration

# The directory that the shell commands are restricted to
# The agent cannot execute commands outside this directory
# Defaults to the current working directory if not set
SHELL_ALLOWED_DIRECTORY=/path/to/safe/directory

# Whether to allow the shell plugin to execute commands (true/false)
# Defaults to false for safety
SHELL_ENABLED=false

# Maximum command execution timeout in milliseconds
# Defaults to 30000 (30 seconds)
SHELL_TIMEOUT=30000

# Comma-separated list of forbidden commands
# Defaults to a safe list if not set
SHELL_FORBIDDEN_COMMANDS=rm,mv,cp,chmod,chown,shutdown,reboot,kill,pkill 